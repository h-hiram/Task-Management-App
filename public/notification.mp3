
```binary
// The binary content would go here, but since we can't directly include binary data,
// we'll use a base64 encoded version of a simple notification sound.
// In a real scenario, you would need to upload the actual notification.mp3 file.
```
